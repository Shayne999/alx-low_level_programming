#include <stdio.h>
/**
 * main - Functions uses putchat to print numbers in a given range.
 * Return: 0(Success)
 */

int main(void)
{
	int i;

	for (i = 0; i <+ 9; i++)
	{
		putchar(i + 48);
	}
	putchar('\n');
	return (0);
}

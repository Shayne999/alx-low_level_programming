#include <stdio.h>
/**
 *main - A pogram that uses the printf function to print out a line.
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
